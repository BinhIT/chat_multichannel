import bcrypt from 'bcryptjs';
import { raw } from 'body-parser';
import db from '../models/index';
let HandlesMessageText = (sender_psid,message) => {
 console
}
module.exports = {
  HandlesMessageText:HandlesMessageText,
  };